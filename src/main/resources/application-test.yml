
mybatis:
  mapper-locations: classpath:mapper/*.xml #对应你的mapper.xml映射文件
  type-aliases-package: com.springboot.Entity #对应实体类

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/adventure?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver