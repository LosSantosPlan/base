mybatis:
  mapper-locations: classpath:mapper/*.xml #对应你的mapper.xml映射文件
  type-aliases-package: com.springboot.Entity #对应实体类

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/base?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB

